import styles from "./Instruction.module.scss";

const Instruction = () => (
  <div className={styles.container}>
    <h1>Как подключить подмену контента на сайт</h1>

    <section style={{ fontSize: "1.02em" }}>
      <p>
        <b>content-injector</b> — это сервис, который позволяет менять картинки
        и текст на вашем сайте без переписывания кода.
        <br />
        <span style={{ fontSize: "0.98em", color: "#666" }}>
          Например, вы хотите, чтобы на одном сайте имя врача, текст акции и
          фото менялись автоматически — просто вставьте несколько специальных
          атрибутов, и управляйте всеми изменениями из личного кабинета.
        </span>
      </p>

      <div
        style={{
          background: "#fff8e1",
          border: "1px solid #ffd700",
          borderRadius: 8,
          padding: 12,
          margin: "18px 0",
        }}
      >
        <b>Что такое кампания?</b> <br />
        Кампания — это набор замен для конкретного сайта или посадочной
        страницы. Для каждой кампании создаётся свой уникальный <b>ID</b>,
        который вы вставляете в скрипт (см. ниже).
        <br />
        <br />
        <b>Что такое ключ и значение?</b>
        <br />
        <b>Ключ</b> — это уникальное название для места, где будет меняться
        контент (например, <code>inject-img-1</code> или{" "}
        <code>inject-text-1</code>).
        <br />
        <b>Значение</b> — то, чем вы хотите заменить (новый текст или ссылка на
        новую картинку).
        <br />
        <br />
        <b>Что такое whitelist?</b>
        <br />
        Это список доменов, где разрешена подмена для кампании. Вы сами
        контролируете, где ваши замены будут работать.
      </div>
    </section>

    <h2>1. Как подключить подмену?</h2>
    <p>
      Вставьте <b>оба этих блока</b> в ваш <code>&lt;head&gt;</code> (или прямо
      перед <code>&lt;/head&gt;</code>):
    </p>
    <pre>
      {`<script src="https://content-injector.vercel.app/loader.js" 
defer data-campaign="ВАШ_CAMPAIGN_ID"></script>
<style>
[data-inject] {
  opacity: 0 !important;
}
[data-inject].injector-visible {
  opacity: 1 !important;
}
</style>`}
    </pre>
    <p>
      <b>ВАЖНО!</b> <br />
      Замените <b>ВАШ_CAMPAIGN_ID</b> на тот, что вы видите в панели управления
      сервисом. Стили не трогайте — они нужны для плавной и незаметной подмены.
    </p>

    <h2>2. Как работает подмена изображений?</h2>
    <p>Найдите нужное изображение. Например:</p>
    <pre>{`<img src="default.jpg" alt="main" />`}</pre>
    <p>
      Добавьте <code>data-inject</code> с нужным <b>ключом</b> (например,{" "}
      <code>inject-img-1</code>):
    </p>
    <pre>{`<img src="default.jpg" data-inject="inject-img-1" alt="main" />`}</pre>
    <p>
      <b>Теперь эта картинка будет подменяться автоматически.</b>
      <br />
      <span style={{ fontSize: "0.96em", color: "#666" }}>
        Если подмены не будет — останется дефолтная.
      </span>
    </p>

    <h2>3. Как подменять текст?</h2>
    <p>Найдите место, где нужно менять текст. Например:</p>
    <pre>{`<h1>Автор: Иван Иванов</h1>`}</pre>
    <p>
      Вырежьте только имя, оберните его в <code>&lt;span&gt;</code> и добавьте{" "}
      <code>data-inject</code> с уникальным <b>ключом</b>:
    </p>
    <pre>{`<h1>Автор: <span data-inject="inject-text-1">Иван Иванов</span></h1>`}</pre>
    <p>
      <b>Важное правило:</b> Если меняется только часть текста (например, имя,
      сумма, город) — заменяйте только этот фрагмент через{" "}
      <code>&lt;span data-inject&gt;...</code>
    </p>

    <h2>4. Итог — что надо запомнить</h2>
    <ul>
      <li>
        <b>Кампания</b> — это уникальный набор замен для вашего сайта.
      </li>
      <li>
        <b>Ключ</b> — уникальное имя для каждой картинки или текста, который вы
        хотите менять.
      </li>
      <li>
        <b>Значение</b> — новый текст или картинка.
      </li>
      <li>
        <b>Скрипт и стили</b> подключаются один раз для всего сайта.
      </li>
      <li>
        <b>Все изменения вы управляете через панель</b> — не нужно переписывать
        сайт для каждого кейса.
      </li>
    </ul>

    <div
      style={{
        marginTop: 32,
        padding: 16,
        background: "#e7f4fa",
        borderRadius: 8,
        border: "1px solid #b6e1fb",
        color: "#2b4e6f",
      }}
    >
      <b>Готово!</b> <br />
      Теперь ваш сайт сам будет менять нужные изображения и тексты без
      редактирования кода. Просто управляйте кампаниями через сервис.
    </div>
  </div>
);

export default Instruction;
